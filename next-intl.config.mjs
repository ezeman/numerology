/**
 * Runtime config for next-intl (App Router)
 * Must be .js/.mjs so the server can load it in production.
 */
export default {
  locales: ['th', 'en'],
  defaultLocale: 'th',
  localePrefix: 'as-needed'
};

